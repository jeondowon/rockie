# Rockie — 기획 & 작업 현황 문서

deskPet 캐릭터에 성향 발견형 성장 요소를 추가한다. 사용자는 온보딩과 일일 질문에 답하고, 누적 답변에 따라 조약돌이 4종의 원석 중 하나로 확정된다. 이후 E/I 질문과 호감도 조건을 거쳐 변성체와 보석으로 진화한다.

> **2026-08-21 기준으로 코드를 다시 읽고 재정리한 문서다.** 앱은 v1.0.2로 배포 중이다.
> 확인 기준: `src/main/`(main·evolution·questions·store·updater·i18n·keyblocker·dock-tracker),
> `src/renderer/pet/pet.js`, `src/renderer/tray/*`, `test/*.test.js`.
> 배포·서명·릴리스 절차는 이 문서가 아니라 `docs/release.md`에 있다.

---

## 0. 현재 요약

### 구현 완료

| 영역 | 완료 내용 |
| --- | --- |
| 온보딩 | 운석 프롤로그, 온보딩 질문 4개, 권한 안내 화면, 온보딩 중 펫 창 잠금, 완료 후 일일 질문 자동 채움 |
| 질문 데이터 | 온보딩 4개, 본 질문 12개, 본 질문 타이브레이커 6개, E/I 질문 6개, E/I 타이브레이커 1개 (전부 한/영 양쪽) |
| 판정 로직 | 0→1 원석 확정, 본 질문 2개·3개 이상 동점 타이브레이커, 1→2 E/I 확정, E/I 동점 타이브레이커 |
| 진화 | 0~3단계, pending 진화 상태 저장, 카드형 진화 연출, 완료 단계 기록 |
| 호감도 | 0~100 상한, 이름 최초 설정 보상, 닦아주기/쓰다듬기 하루 1회 각 +3, 90점 도달 시 2→3 진화 |
| 일일 질문 | 오전 8시 리셋, 미답 질문 유지, 부족분 보충, 단계별 하루 한도 적용, 트레이 배지 |
| 질문 UI | 트레이 PET 화면의 버튼으로 펫 옆 질문 카드 오픈, 상황 카드 → 질문 카드 → 답변 완료 카드 흐름 |
| 트레이 UI | MENU/ONBOARDING/PET/SYSTEM/SETTINGS, 이름 편집, 진행도, 성향 태그, 답변 히스토리, 돌보기 버튼 |
| 스킨 | 0~3단계 진화 모습을 스킨처럼 착용/해제, 잠금/착용중 표시, 펫 창과 트레이 히어로 반영 |
| 모드 | 펫 더블클릭 옵션창 → 키보드 청소 / 집중(일시정지 가능) / 쪽잠(잠금 + 알람). 트레이 배너에 미러링 |
| 시스템 모니터 | CPU/RAM/디스크/배터리/네트워크 조회, SYSTEM 화면 폴링, 부하별 반응 카드 |
| AI 사용량 | SYSTEM 화면의 Codex 사용량 행. 로컬 로그만 읽어 표시하고 창을 열 때·수동 새로고침에만 갱신 |
| 위치/크기 | 설정의 따라오기/좌하단/우하단, 작게/보통/크게, 롱프레스 드래그 |
| 화면 캡처에서 숨기기 | 설정 토글(기본 꺼짐). 켜면 펫 창을 `setContentProtection`으로 캡처 대상에서 제외해 스크린샷·화면 녹화·화면 공유에 애완돌이 안 찍힘. 내 모니터에는 그대로 보임 |
| 다국어 | 한국어/영어. 설정에서 즉시 전환, 온보딩 화면에서도 전환 가능 |
| 자동 업데이트 | `electron-updater` + GitHub Releases. 백그라운드 다운로드 후 트레이 항목으로 설치 |
| 부가 리액션 | 활성 앱 말풍선, SNS 체류 단계 멘트, 배터리 부족 리액션, 시간대/장시간 사용 리액션 |
| 테스트 | `node --test` 32개 통과 (진화·타이브레이커·온보딩·이름 보상·성향 태그·저장 마이그레이션·AI 사용량) |
| 배포 | macOS Developer ID 서명 + notarization, dmg/zip, GitHub Releases 공개. v1.0.2 배포 완료 |
| 웹사이트 | `jeondowon.com/rockie` 배포 페이지 + 정책 3종 게시. 앱 안에서 링크로 연다 |

### 미구현 — 추후 검토

| 영역 | 미구현 내용 |
| --- | --- |
| 리소스 구조 개선 | GIF → PNG 시퀀스 + Canvas, 팔레트 스왑, 레이어 합성 (16절) |
| 확장 디스플레이 | 주 모니터에만 잔류하는 현재 동작 유지. 전 디스플레이 이동은 미정 |
| 앱별 말풍선 토글 | 설정에서 끄는 스위치가 아직 없다 (`docs/release.md` 7절) |
| 설치 안내 문서 | `docs/install.md` 미작성 |
| 정책 문서 영문판 | 앱은 영어를 지원하는데 정책 링크는 한국어 페이지로 간다 |
| Windows | 보류. 근거와 착수 순서는 `docs/release.md` 6.3 |

### 의도적 제외

| 영역 | 상태 |
| --- | --- |
| Mac App Store | **제외 확정.** 샌드박스에서 핵심 기능 4개가 불가능하다. 근거는 `docs/release.md` 1.2 |
| 발표 모드 | 제거됨(커밋 `63ee5f6`). 화면 공유 대응은 설정의 "화면 캡처에서 숨기기" 토글이 대신한다 |
| 트레이 내부 질문 모달 | 보류/제외. 실제 답변은 펫 옆 오버레이를 정본으로 유지 |
| 별도 코스메틱 스킨 | 제외. 스킨은 진화로 해금한 단계별 모습 자체만 사용 |
| 레벨별 아이템 언락 | 구현 안 함 결정. 호감도 레벨은 표시 전용 |
| 상단 모서리 위치 고정 | 제외. 현재 하단 동선과 맞지 않아 좌하단/우하단만 제공 |
| 날씨 리액션 | 제외. 위치 권한/API 키/네트워크 실패 처리가 필요해 현재 범위에서는 빼기로 결정 |
| Claude Code 사용량 | 제외 확정. 로컬 어디에도 한도 값이 없다. 근거는 12절 |
| macOS 시스템 집중 모드 연동 | 제거됨. 앱 자체 집중 모드만 남긴다 |
| 채팅 | 로컬 LLM 연동, 스트리밍 IPC, 말투 프롬프트, 대화 요약 압축 (17절) |
| 아이템 인벤토리 | 모자 등 장착 아이템 자산, 언락, 장착 UI |

---

## 1. 핵심 컨셉

- 캐릭터는 설치 직후 조약돌 `Rockie`로 시작한다.
- 첫 실행 시 트레이 팝업에서 운석 프롤로그 온보딩을 진행한다.
- 온보딩 질문 4개도 0→1 원석 판정 점수에 반영된다.
- 온보딩 완료 후 일일 질문이 열리고, 답변 누적에 따라 돌 종류가 확정된다.
- 돌 종류와 E/I 판정은 사용자가 직접 고르는 값이 아니라 답변 결과로 발견된다.
- 진화 단계는 역행하지 않는다. 단, 사용자는 이미 도달한 단계의 모습을 스킨으로 다시 표시할 수 있다.

---

## 2. 진화 단계

| 단계 | 명칭 | 진입 조건 | 구현 상태 |
| --- | --- | --- | --- |
| 0단계 | Rockie / 조약돌 | 기본 상태 | 완료 |
| 1단계 | 원석 4종 | 온보딩 완료 + 본 질문 12개 완료 + 돌 판정 | 완료 |
| 2단계 | 변성체 8종 | E/I 질문 6개 완료 + E/I 판정 | 완료 |
| 3단계 | 보석 8종 | 2단계 상태에서 호감도 90 이상 | 완료 |

### 2.1 원석 매핑

| 돌 종류 | MBTI 기질군 | 성향 특성 | 구현 key |
| --- | --- | --- | --- |
| 화강암 | SJ | 원칙, 안정, 책임 | `granite` |
| 현무암 | SP | 즉흥, 행동, 현재 중심 | `basalt` |
| 대리석 | NF | 공감, 감성, 의미 | `marble` |
| 편마암 | NT | 논리, 전략, 구조 | `gneiss` |

### 2.2 돌별 성향 정리

| 돌 | 핵심 성향 | 자주 드러나는 판단 기준 | 강점으로 보이는 모습 | 과해질 때의 인상 |
| --- | --- | --- | --- | --- |
| 화강암 | 안정적이고 책임감 있는 계획형 | 기준, 순서, 약속, 안전성, 검증된 방법 | 맡은 일을 끝까지 해내고, 상황을 차분하게 정리한다 | 너무 조심스럽거나 변화에 늦게 반응하는 것처럼 보일 수 있다 |
| 현무암 | 즉흥적이고 에너지 있는 실행형 | 지금의 감각, 현장 반응, 기회, 빠른 시도 | 망설임 없이 시작하고, 분위기를 움직이며, 변화에 빠르게 적응한다 | 충분히 살피기 전에 움직이거나 가볍게 결정하는 것처럼 보일 수 있다 |
| 대리석 | 감정과 의미를 중시하는 공감형 | 사람의 마음, 관계의 편안함, 의미, 정서적 균형 | 주변 사람을 세심하게 살피고, 말과 분위기의 온도를 잘 읽는다 | 결정을 미루거나 감정의 영향을 크게 받는 것처럼 보일 수 있다 |
| 편마암 | 구조를 파악하는 분석형 | 원인, 논리, 효율, 전체 구조, 장단점 비교 | 복잡한 문제를 쪼개 보고, 핵심 흐름과 근거를 잘 정리한다 | 차갑거나 지나치게 따지는 것처럼 보일 수 있다 |

질문 선택지는 이 성향을 직접 노출하지 않고, 사용자의 자연스러운 선택 기준을 통해 점수로 누적한다. 예를 들어 화강암 선택지는 계획·기준·책임을, 현무암 선택지는 즉시 시도·현장감·활력을, 대리석 선택지는 마음·관계·의미를, 편마암 선택지는 구조·원리·분석을 주로 반영한다.

### 2.3 계열별 진화 매핑

| 원석 | 변성체 | 외향 3단계 | 내향 3단계 |
| --- | --- | --- | --- |
| 화강암 | 페그마타이트 | 토파즈 | 아쿠아마린 |
| 현무암 | 에클로자이트 | 브릴리언트 컷 다이아몬드 | 원석 다이아몬드 |
| 대리석 | 코런덤 대리석 | 파티 사파이어 | 루비 |
| 편마암 | 미그마타이트 | 라브라도라이트 | 문스톤 |

각 단계의 성향 설명과 진화 근거 문구는 `docs/evolution.md`에, 상태 → GIF 접두어 매핑은 `src/renderer/shared/sprites.js`에 있다.

### 2.4 진화 연출

구현 완료:

- 진화 조건 충족 시 `pet.pendingEvolution` 저장
- 펫 클릭 시 진화 카드 오픈
- 기존 모습 fade/blink 후 새 모습 표시
- `completePendingEvolution` 호출 후 pending 해제
- 완료 단계는 `presentedEvolutionStages`에 기록
- 진화 완료 시 착용 중이던 이전 스킨 해제

---

## 3. 질문 구성

현재 질문 개수는 코드 기준으로 다음과 같다.

| 구분 | 개수 | 위치 | 판정 반영 |
| --- | --- | --- | --- |
| 온보딩 질문 | 4개 | `ONBOARDING_QUESTIONS` | 돌 점수에 반영 |
| 본 질문 | 12개 | `MAIN_QUESTIONS` | 돌 점수에 반영 |
| 본 질문 타이브레이커 | 6개 | `TIEBREAKERS` | 동점 시 돌 점수에 반영 |
| E/I 질문 | 6개 | `EI_QUESTIONS` | 외향/내향 점수에 반영 |
| E/I 타이브레이커 | 1개 | `EI_TIEBREAKER` | E/I 3:3 동점 시 반영 |

표시용 전체 진행도는 `온보딩 4 + 본 질문 12 + E/I 질문 6 = 22`로 계산한다. 타이브레이커는 표시용 전체 개수에 포함하지 않는다.

**모든 질문의 상황·질문·선택지 문구는 `{ ko, en }` 양쪽을 갖는다.** 아래 표는 한국어만 옮긴 것이며, 영어 문구는 `src/main/questions.js`를 직접 봐야 한다. `category`와 `traitTag`는 성향 태그 계산용 내부 값이라 번역하지 않는다.

### 3.1 온보딩 질문

구현 완료:

- 운석 낙하/착지/소리/균열/조약돌 등장 흐름
- 각 장면 사이에 4개 질문 삽입
- 답변은 `traits.traitScores`에 즉시 반영
- `mainQuestionProgress`는 증가하지 않음
- 4개 모두 답해야 온보딩 완료 가능
- 온보딩 완료 후 오늘 질문 3개를 채움

### 3.2 본 질문

구현 완료:

- 본 질문 12개는 온보딩 이후 일일 질문으로 노출
- 각 선택지는 화강암/현무암/대리석/편마암 중 하나에 +1
- `mainQuestionProgress`가 12에 도달하면 원석 판정 시도
- 본 질문 문항은 기존 추상 질문이 아니라 상황 기반 질문으로 교체됨

### 3.3 E/I 질문

구현 완료:

- 원석 1단계 확정 후 E/I 질문이 노출됨
- E/I 질문은 6개
- 외향/내향 점수 중 높은 쪽으로 `evolutionVariant` 확정
- 3:3 동점이면 E/I 타이브레이커 1개를 먼저 삽입

### 3.4 현재 질문 리스트

아래 목록은 `src/main/questions.js` 기준이다.

#### 온보딩 질문 4개

| ID | 카테고리 | 상황 | 질문 | 화강암 | 현무암 | 대리석 | 편마암 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `onboarding_01` | 첫 반응 | 떨어진 운석에서 아직 연기가 조금 피어오르고, 주변은 조용합니다. | 가장 먼저 어떻게 할까요? | 주변이 안전한지 확인하고 천천히 다가간다 | 일단 가까이 가서 직접 살펴본다 | 혹시 누군가 다친 건 아닌지 걱정된다 | 떨어진 방향과 흔적을 보고 정체를 추측한다 |
| `onboarding_02` | 호기심 | 운석 안쪽에서 아주 작은 소리가 계속 들립니다. / ....달그락... 달그락... | 이 소리를 들은 나는? | 섣불리 건드리지 않고 상태를 더 지켜본다 | 두근거려서 바로 확인해보고 싶다 | 안에 갇힌 존재가 있다면 꺼내주고 싶다 | 소리의 간격과 방향을 살펴 안쪽 구조를 상상한다 |
| `onboarding_03` | 변화 대응 | 운석의 금이 점점 벌어지고, 안쪽에서 무언가 움직입니다. | 나는 무엇을 준비할까요? | 조각이 튈 수 있으니 안전한 거리를 확보한다 | 바로 받아낼 수 있게 가까이에서 기다린다 | 놀라지 않도록 조용히 말을 걸어본다 | 금이 퍼지는 모양을 보고 어느 쪽이 열릴지 본다 |
| `onboarding_04` | 첫 관계 | 조약돌이 나에게 말을 건네네요. / “헉! 안녕하세요, 주인님!” | 나는 어떻게 대답할까요? | 괜찮아? 우선 안전한 곳으로 가자 | 우와, 너 진짜 살아있는 돌이야? | 많이 무서웠지? 이제 괜찮아 | 너는 어디서 왔고, 어떻게 말할 수 있어? |

#### 본 질문 12개

| ID | 카테고리 | 상황 | 질문 | 화강암 | 현무암 | 대리석 | 편마암 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `main_01` | 계획/실행 | 한 달 뒤 중요한 발표가 있어요. 자료 조사, 슬라이드 제작, 발표 연습까지 준비할 일이 꽤 많습니다. | 나는 준비를 어떻게 시작할까요? | 마감일까지 할 일을 일정표로 먼저 나눈다 | 일단 자료를 열고 손이 가는 것부터 시작한다 | 듣는 사람에게 어떤 메시지가 닿을지 먼저 생각한다 | 핵심 주장과 근거 구조를 먼저 잡는다 |
| `main_02` | 관계/협업 | 친구가 고민을 털어놓고 있어요. 해결책을 원하는지, 그냥 들어주길 원하는지 바로 알기 어렵습니다. | 내가 먼저 보일 반응은? | 필요하면 해결 방법을 같이 정리해보자고 말한다 | 기분이 조금 풀릴 만한 일을 함께 찾아본다 | 먼저 충분히 들어주고 감정을 받아준다 | 상황의 원인과 선택지를 차분히 짚어본다 |
| `main_03` | 가치관 | 새로운 규칙이 생겼는데, 효율은 좋아질 것 같지만 누군가에게는 꽤 불편할 수도 있어요. | 내가 먼저 따져보고 싶은 것은? | 규칙이 공정하고 일관되게 적용되는지 | 현장에서 실제로 잘 굴러가는지 | 불편을 겪는 사람이 얼마나 되는지 | 규칙의 근거가 충분히 논리적인지 |
| `main_04` | 일상/감정 | 오랜만에 아무 일정이 없는 휴일이에요. 해야 하는 일도 조금 있지만, 마음은 쉬고 싶어 합니다. | 이럴 때 가장 편하게 느껴지는 선택은? | 정리할 일을 끝내고 마음 편히 쉰다 | 그날 끌리는 곳이나 활동을 바로 따라간다 | 좋아하는 사람이나 콘텐츠로 마음을 채운다 | 혼자 깊이 몰입할 주제를 잡는다 |
| `main_05` | 문제해결 | 팀 프로젝트에서 자료가 너무 많이 모였어요. 좋은 내용도 많지만, 발표 시간은 짧습니다. | 나는 무엇부터 정하자고 말할까요? | 필수 내용과 선택 내용을 나누자 | 짧게 발표해보며 반응 좋은 내용을 고르자 | 듣는 사람이 따라오기 쉬운 흐름을 고르자 | 주장과 근거가 이어지는 구조를 만들자 |
| `main_06` | 계획/실행 | 친구들이 갑자기 여행을 가자고 해요. 날짜는 맞지만, 숙소와 예산은 아직 거의 정해지지 않았습니다. | 내 마음에 가까운 반응은? | 큰 항목은 정하고 가야 마음이 편하다 | 이런 건 타이밍이니까 일단 가보고 싶다 | 같이 가는 사람들이 모두 편한지가 제일 중요하다 | 예산, 이동, 일정의 장단점을 비교해보고 싶다 |
| `main_07` | 관계/협업 | 모임에서 의견이 둘로 갈렸고, 말수가 적은 사람들은 아직 자기 생각을 말하지 않았어요. | 내가 맡게 될 가능성이 큰 역할은? | 결정해야 할 기준과 순서를 잡는다 | 어색함을 깨고 사람들이 편하게 말하게 만든다 | 말하지 못한 사람의 의견도 챙긴다 | 양쪽 의견의 핵심 차이를 정리한다 |
| `main_08` | 가치관 | 누군가 빠르게 성과를 내는 모습을 봤어요. 멋져 보이지만, 그 과정이 조금 불안정해 보이기도 합니다. | 나는 어떤 성공이 더 마음에 드나요? | 느려도 오래 갈 수 있는 안정적인 성공 | 좋은 기회를 잡아 크게 도약하는 성공 | 내가 중요하게 여기는 의미를 지키는 성공 | 원리와 시스템을 만들어내는 성공 |
| `main_09` | 일상/감정 | 하루 종일 여러 사람을 만나고 집에 돌아왔어요. 즐거웠지만 은근히 피곤함도 남아 있습니다. | 집에 도착한 뒤 가장 먼저 하고 싶은 것은? | 씻고 정리하며 원래 리듬으로 돌아온다 | 오늘의 즐거웠던 장면을 다시 떠올린다 | 좋았던 말과 마음을 천천히 곱씹는다 | 혼자 있으면서 생각을 차분히 정리한다 |
| `main_10` | 문제해결 | 처음 보는 장비를 써야 합니다. 설명서는 길고, 옆 사람은 몇 번 눌러보면 금방 익숙해진다고 해요. | 나는 어떻게 익히는 편인가요? | 기본 사용법을 먼저 읽고 따라 한다 | 직접 만져보며 감을 잡는다 | 다른 사람이 어디서 어려워하는지 보며 배운다 | 왜 그렇게 작동하는지 구조를 이해한다 |
| `main_11` | 계획/실행 | 해야 할 일이 세 개나 겹쳤어요. 하나는 급하고, 하나는 중요하고, 하나는 하고 싶은 일입니다. | 내가 먼저 고를 기준에 가까운 것은? | 마감과 책임이 분명한 일부터 한다 | 지금 가장 탄력 받을 수 있는 일부터 한다 | 내 마음이 무너지지 않을 순서를 고른다 | 전체 결과가 가장 좋아지는 순서를 따져본다 |
| `main_12` | 자기인식 | 가까운 사람이 나의 장점을 한 문장으로 말해준다고 해요. 어떤 말을 들으면 가장 나답다고 느낄까요? | 내가 가장 고개를 끄덕일 말은? | 너는 맡은 일을 끝까지 해내는 사람이야 | 너는 상황을 즐기고 빠르게 움직이는 사람이야 | 너는 사람의 마음과 의미를 잘 보는 사람이야 | 너는 흐름을 읽고 구조를 세우는 사람이야 |

#### 본 질문 타이브레이커 6개

| ID | 상황 | 질문 | 선택지 A | 선택지 B |
| --- | --- | --- | --- | --- |
| `tb_granite_basalt` | 행사 시작 10분 전, 준비한 순서표와 현장 상황이 다르게 흘러가고 있어요. 바로 진행 방식을 정해야 합니다. | 나는 먼저 어떻게 움직일까요? | 순서표를 빠르게 다시 확인하고 최소한의 기준을 맞춘다 (화강암) | 현장 분위기에 맞춰 바로 진행하며 중간에 조정한다 (현무암) |
| `tb_granite_marble` | 함께 준비하던 친구가 많이 지쳐 보여요. 계획대로라면 오늘 마무리해야 하지만, 친구는 잠깐 쉬고 싶어 합니다. | 나는 어떤 선택을 할까요? | 남은 일을 작게 나눠 꼭 필요한 부분부터 끝내자고 한다 (화강암) | 친구의 상태를 먼저 살피고 쉬어갈 방법을 같이 찾는다 (대리석) |
| `tb_granite_gneiss` | 반복해서 같은 실수가 생기는 작업이 있어요. 체크리스트도 있고, 왜 반복되는지 원인도 궁금합니다. | 나는 무엇부터 할까요? | 우선 체크리스트를 더 꼼꼼히 따라 빠진 단계를 막는다 (화강암) | 실수가 반복되는 지점을 찾아 작업 흐름 자체를 다시 본다 (편마암) |
| `tb_basalt_marble` | 친구들이 갑자기 밤 산책을 가자고 해요. 재미있을 것 같지만, 한 친구는 조금 망설이고 있습니다. | 나는 어떻게 반응할까요? | 분위기를 살려 일단 나가보자고 신나게 제안한다 (현무암) | 망설이는 친구가 불편하지 않은지 먼저 물어본다 (대리석) |
| `tb_basalt_gneiss` | 새 앱을 처음 써보게 됐어요. 화면에는 여러 버튼이 보이고, 옆에는 기능 설명 문서도 열려 있습니다. | 나는 어떻게 시작할까요? | 버튼을 직접 눌러보며 어떤 기능인지 감을 잡는다 (현무암) | 기능 설명을 먼저 훑고 구조를 이해한 뒤 써본다 (편마암) |
| `tb_marble_gneiss` | 팀원이 낸 아이디어가 회의에서 바로 반박당했어요. 분위기는 어색해졌고, 아이디어의 문제점도 정리해야 합니다. | 나는 먼저 무엇을 할까요? | 아이디어를 낸 사람이 상처받지 않게 분위기를 먼저 풀어준다 (대리석) | 반박된 이유를 항목별로 정리해 개선점을 찾는다 (편마암) |

#### E/I 질문 6개

| ID | 카테고리 | 상황 | 질문 | 외향 | 내향 |
| --- | --- | --- | --- | --- | --- |
| `ei_01` | 에너지 | 힘든 하루가 끝났어요. 몸은 피곤하지만 마음을 회복할 시간이 필요합니다. | 나는 어떤 방식으로 에너지를 되찾나요? | 좋아하는 사람과 이야기하며 기분을 풀어낸다 | 혼자 조용히 시간을 보내며 정리한다 |
| `ei_02` | 관계 시작 | 처음 보는 사람들이 모인 자리입니다. 대화는 아직 시작되지 않았고 다들 분위기를 살피고 있어요. | 나는 보통 어떻게 시작하나요? | 먼저 말을 걸고 분위기를 만든다 | 상황을 지켜보다 편해지면 다가간다 |
| `ei_03` | 생각 정리 | 머릿속에 생각이 많아져서 정리가 필요합니다. 혼자 붙잡을 수도, 밖으로 꺼낼 수도 있어요. | 내게 더 자연스러운 생각 정리 방식은? | 누군가에게 이야기하며 정리한다 | 글로 쓰거나 혼자 되짚어본다 |
| `ei_04` | 작업 환경 | 집중해서 작업해야 합니다. 주변에 사람이 있는 공간과 완전히 조용한 공간 중 고를 수 있어요. | 나는 어디서 더 집중이 잘 되나요? | 카페처럼 적당한 활기가 있는 곳 | 방해받지 않는 조용한 곳 |
| `ei_05` | 표현 방식 | 좋은 일이 생겼어요. 아직 아무에게도 말하지 않았지만 기분이 꽤 좋습니다. | 나는 이 기쁨을 어떻게 나누나요? | 바로 누군가에게 알리고 함께 기뻐한다 | 혼자 조용히 그 순간을 음미한다 |
| `ei_06` | 낯선 상황 | 여행지에서 길을 잃었어요. 주변에 사람도 있고, 휴대폰 지도도 켤 수 있습니다. | 나는 먼저 무엇을 할까요? | 근처 사람에게 바로 물어본다 | 지도를 보며 직접 방향을 찾는다 |

#### E/I 타이브레이커 1개

| ID | 상황 | 질문 | 외향 | 내향 |
| --- | --- | --- | --- | --- |
| `eitb_01` | 새로운 관심사가 생겼어요. 혼자 알아볼 수도 있고, 다른 사람들과 연결해볼 수도 있습니다. | 나는 이 관심사를 어떻게 넓혀갈까요? | 관심 있는 사람들과 이야기하며 넓혀간다 | 자료를 찾아보며 혼자 깊이 파고든다 |

---

## 4. 판정 로직

### 4.1 0→1 원석 판정

구현 완료:

1. 온보딩 질문 4개와 본 질문 12개의 돌 점수를 누적한다.
2. 본 질문 12개가 끝나면 최고점 돌을 찾는다.
3. 최고점이 하나면 즉시 1단계로 확정한다.
4. 최고점이 2개 이상이면 필요한 타이브레이커를 `todaysQuestions` 맨 앞에 삽입한다.
5. 타이브레이커 답변 후 다시 판정한다.
6. 모든 타이브레이커 후에도 동점이면 가장 최근 답변 기준으로 확정한다.

테스트 완료:

- 본 질문 완료 후 1단계 확정
- 2개 돌 동점 타이브레이커
- 3개 돌 동점 타이브레이커 순차 삽입

### 4.2 1→2 E/I 판정

구현 완료:

1. E/I 질문 6개 답변을 누적한다.
2. 외향 점수가 높으면 `extrovert` 확정.
3. 내향 점수가 높으면 `introvert` 확정.
4. 3:3이면 `eitb_01`을 `todaysQuestions` 맨 앞에 삽입한다.
5. 타이브레이커 답변 후 2단계로 확정한다.

테스트 완료:

- E/I 질문 완료 후 2단계 확정
- E/I 동점 타이브레이커

### 4.3 2→3 호감도 판정

구현 완료:

- `evolutionStage === 2 && affinityPoints >= 90`이면 3단계 확정
- 2단계 진입 시 이미 90점 이상이면 즉시 3단계로 상승
- 2단계에서 닦아주기/쓰다듬기로 90점 이상 도달해도 3단계로 상승

테스트 완료:

- 2단계 진입 즉시 3단계
- 닦아주기/쓰다듬기 후 3단계

---

## 5. 질문 노출과 알림

### 구현 완료

| 항목 | 현재 구현 |
| --- | --- |
| 접근 방식 | 트레이 PET 화면의 "새로운 질문에 답하기" 버튼 |
| 답변 위치 | 데스크톱 펫 옆 질문 카드 |
| 강제 팝업 | 없음 |
| 스킵 | 없음. 닫으면 질문은 유지됨 |
| 리셋 시각 | 매일 오전 8시 |
| 미답 이월 | 이전 질문 유지 후 부족분만 보충 |
| 배너 알림 | 오전 8시 갱신 후 질문이 있으면 OS 알림 1회 |
| 트레이 배지 | 2단계 전이고 답할 질문이 있으면 표시 |

### 단계별 하루 질문 한도

| 상태 | 하루 한도 | 근거 |
| --- | --- | --- |
| 온보딩 미완료 | 0개 | 온보딩이 먼저 필요 |
| 0단계, 온보딩 완료 | 3개 | `maxDailyQuestions(0) === 3` |
| 1단계 | 2개 | `maxDailyQuestions(1) === 2` |
| 2단계 이상 | 0개 | 질문 종료 |

한도는 `evolution.js`의 `maxDailyQuestions(stage)` 한 곳에서만 정한다.

---

## 6. 저장 스키마

**정본은 `src/main/store.js`의 `defaultData()`이고, 필드별 설명은 `docs/dataschema.md`에 있다.** 여기서는 진화 흐름과 직접 맞물리는 부분만 짚는다.

주요 저장 필드:

- `onboarding.completed`, `onboarding.step`, `onboarding.completedAt`
- `pet.stoneType`, `pet.stoneConfirmedAt`, `pet.evolutionStage`, `pet.evolutionVariant`
- `pet.pendingEvolution`, `pet.presentedEvolutionStages`, `pet.activeSkinStage`
- `traits.traitScores`, `traits.eiScores`, `traits.tiebreaker`
- `questions.mainQuestionProgress`, `questions.eiQuestionProgress`
- `questions.todaysQuestions`, `questions.dailyResetAt`, `questions.answeredQuestions`
- `affinity.affinityPoints`, `affinity.dailyCleanDone`, `affinity.dailyPetDone`
- `notifications.hasUnreadBadge`, `notifications.notificationsEnabled`
- `settings.autoLaunch`, `soundEnabled`, `hideFromCapture`, `petPlacement`, `petSize`, `focusMinutes`, `napMinutes`, `language`

주의할 점:

- **`traitScores`·`eiScores`의 키는 한글 고정이다.** 표시 언어를 영어로 바꿔도 저장 키는 바뀌지 않는다. 언어를 오가며 답해도 점수가 흩어지지 않게 하려는 것이다.
- 저장은 임시 파일 → `rename`(원자적). 읽기 실패 시 원본을 `petdata.corrupt-<ts>.json`으로 보존하고 기본값으로 시작한다.

마이그레이션 보강:

- 기존 데이터에 새 최상위 섹션이나 필드가 없으면 기본값으로 채운다(한 단계 깊이).
- 온보딩 도입 전부터 진행 데이터가 있던 사용자는 온보딩을 완료 처리해 기존 상태를 유지한다. 단 `onboarding.step > 0`(진행 중)이면 제외 — 권한 화면에서 앱이 재시작되는 경로가 있어 로드가 두 번 이상 일어나기 때문이다.
- 두 갈래 모두 `test/store.test.js`가 고정하고 있다.

---

## 7. 트레이 UI

### 구현 완료

| 화면 | 완료 내용 |
| --- | --- |
| MENU | 나의 애완돌 / 시스템 모니터 / 애완돌 숨기기·보이기 / 설정 / (조건부) 업데이트 설치 후 재시작 / 종료. 배지, 메뉴 높이 자동 조절 |
| ONBOARDING | 운석 프롤로그, 질문 선택지, 권한 안내 화면, 언어 전환 버튼, 종료 버튼, 완료 후 PET 진입 |
| PET | 히어로 이미지, 이름 편집, 성향 설명, 태그, 진행도, 질문 버튼, 호감도, 돌보기, 스킨, 히스토리, 후원 카드 |
| SYSTEM | 시스템 수치, 게이지, 상세 드롭다운, 반응 카드, Codex 사용량 행 |
| SETTINGS | 자동실행, 질문 알림, 화면 캡처에서 숨기기, 효과음, 화면 기록 권한, (거부 시에만) 자동화 권한 안내, 펫 위치, 펫 크기, 집중 시간, 쪽잠 시간, 언어, 처음부터 다시 키우기, 홈페이지 링크, 정책 링크 4종, 앱 버전 |

### 모드 배너

집중·청소·쪽잠 모드에 들어가면 트레이 상단에 배너가 뜬다. 모드 아이콘·이름·남은 시간과 "일시정지 / 계속하기"·"종료" 버튼이 있다.

- 상태 원본은 펫 렌더러다. `mode:set-status`로 메인에 올리고, 메인이 `mode:status`로 트레이에 미러링한다.
- 버튼을 누르면 반대로 `exit-mode`·`pause-mode` 액션이 메인을 거쳐 펫 렌더러로 간다.
- 남은 시간 카운트다운은 트레이가 `focusEndAt`으로 직접 계산해 표시한다(초당 IPC를 주고받지 않는다).
- 펫 렌더러가 리로드되면 모드 상태가 초기화되므로, 메인이 `releaseModeLocks()`로 배너와 잠금을 함께 정리한다.

### 설정 화면 세부

- 위치 `follow`/`bottom-left`/`bottom-right`, 크기 `small`/`medium`/`large` — 칩 선택.
- 집중 시간은 칩 4개(15·25·45·60분), 쪽잠 시간은 슬라이더(10분 배수 ±2분에 자석 스냅, 손을 뗄 때만 저장).
- 언어 칩(한국어/English)은 즉시 전환된다. 저장 데이터의 키는 바뀌지 않는다.
- 화면 기록 권한 행은 첫 클릭에 시스템 권한 요청을 보내고, 두 번째 클릭에서만 시스템 설정을 연다 — 요청 후에도 허용으로 안 읽히면 "허용했지만 재시작 전"인지 "거부"인지 구분할 수 없기 때문이다.
- 자동화 권한 행은 **거부 상태일 때만** 보인다. 허용이면 할 일이 없고, `unknown`이면 판단 근거가 없다.
- 앱 버전은 `settings:get` 응답에 얹혀 오는 `appVersion`이다. `data-i18n`이 없는 `<span>`에 따로 넣는다 — `applyStaticI18n`이 `data-i18n-html` 요소의 innerHTML을 통째로 덮어써서, 안 그러면 언어를 바꿀 때 지워진다.

### PET 화면 세부 상태

구현 완료:

- 현재 펫 창 스프라이트 포즈를 트레이 히어로와 SYSTEM 반응 카드에 동기화
- 단계별 상태 라벨과 진화 근거 표시
- `STONE_TRAIT` 대신 답변 기반 카테고리별 성향 태그를 계산해 표시
- 답변 히스토리를 최신순으로 표시
- 이름 수정 시 사용자 이름/애완돌 이름 최초 설정 보상 반영

보류:

- 모자 등 별도 아이템 인벤토리

---

## 8. 펫 창 UI

### 구현 완료

- 커서 따라오기
- 좌하단/우하단 고정 위치
- 크기 3단계
- Dock 회피
- 롱프레스 드래그 이동
- 클릭 말풍선
- 시간대별 클릭 말풍선
- 장시간 사용 시 휴식 권유 클릭 말풍선
- 밤 시간대 sleepy 기본 포즈
- 활성 앱별 말풍선
- 배터리 부족 리액션
- 돌보기 피드백: 닦아주기 시 smile, 쓰다듬기 시 heart 오버레이
- 질문 카드
- 진화 카드
- 더블클릭 모드 옵션창(첫 클릭 때 안내 말풍선 1회)
- 온보딩 전 펫 창 잠금

### 클릭 통과 처리

펫 창은 평소 `setIgnoreMouseEvents(true)`로 클릭을 통과시킨다. 클릭을 받아야 하는 요소는 두 방식으로 나뉜다.

- 캐릭터·질문 카드·모드 패널·집중 pill: `mouseenter`에 캡처, `mouseleave`에 통과 복귀.
- 청소·쪽잠 오버레이: 화면 전체를 덮고 캡처를 유지한다. 모드 중에는 `mouseleave`가 와도 통과로 되돌리지 않는다(옵션창이 사라진 뒤 늦게 오는 `mouseleave`에 오버레이가 클릭을 잃는 버그가 있었다).

### 드래그 상태

- 조건: 모든 위치 모드에서 동작한다.
- 방식: 캐릭터를 200ms 이상 누르면 드래그 시작.
- `follow` 모드에서는 놓은 뒤 3초 동안 그 위치에 머문 뒤 다시 커서를 따라간다.
- 질문 카드, 진화 카드, 온보딩 잠금 중에는 드래그하지 않는다.

---

## 9. 모드

펫을 **더블클릭**하면 옵션창이 열리고 세 가지 모드를 고를 수 있다. 모드 UI는 전부 펫 창 안에서 자족적으로 처리한다(별도 창 없음). 상태만 트레이 배너로 미러링한다.

| 모드 | id | 하는 일 | 해제 방법 |
| --- | --- | --- | --- |
| 키보드 청소 | `clean` | 화면 전체 오버레이가 클릭을 가로채고, 네이티브 헬퍼가 키보드 입력을 차단한다. 시간 제한 없음 | 오버레이의 "닫기" 클릭, 또는 비상 해제 연타 |
| 집중 | `focus` | 방해금지(말풍선·배너 알림 억제). 설정한 시간만큼 카운트다운 | 상단 pill 또는 트레이 배너의 "종료". 일시정지/재개 가능 |
| 쪽잠 | `nap` | 청소와 같은 잠금 + 타이머 + 알람 | 알람의 "끄기" / "5분 후 다시" |

**발표 모드는 제거됐다**(커밋 `63ee5f6`). 화면 공유 대응은 설정의 "화면 캡처에서 숨기기" 토글이 대신한다.

### 키보드 잠금 (청소 · 쪽잠)

- 잠금은 `assets/helper/KeyBlocker.app`(Swift로 작성한 별도 번들)이 담당한다. 메인의 `src/main/keyblocker.js`가 `/usr/bin/open`으로 띄우고 `pkill`로 내린다.
- **필요한 권한은 '손쉬운 사용' 하나뿐이다.** 입력 모니터링·HID seize 경로는 전부 삭제됐다.
- 권한이 없으면 오버레이를 화면을 덮지 않는 작은 카드로 줄이고, 그 권한 항목을 바로 여는 버튼을 띄운다 — 화면을 덮으면 뒤에 열리는 시스템 설정을 보며 조작할 수 없다.
- 앱이 멈추면 키보드가 영영 잠긴 채 남으므로, 렌더러가 10초마다 하트비트를 보내 메인의 자동 해제 상한(30초)을 밀어준다. 렌더러가 죽으면 30초 안에 잠금이 풀린다.
- 쪽잠은 청소와 달리 하트비트로 잠금을 재시도하지 않는다. 권한을 켜도 저절로 잠기지 않으니 다시 시작해야 한다는 것과, **잠금과 무관하게 알람은 울린다**는 것을 함께 안내한다.

### 집중 모드

- 길이는 `settings.focusMinutes`(기본 25분, 칩으로 15/25/45/60 선택).
- 방해금지는 렌더러(말풍선)와 메인(배너 알림) 양쪽을 함께 끈다(`pet:set-dnd`).
- 일시정지 중에는 종료 시각(`focusEndAt`) 대신 남은 시간(`focusPausedMs`)이 기준이 된다.

### 쪽잠 알람

- 길이는 `settings.napMinutes`(기본 20분, 슬라이더).
- 알람은 **효과음 설정이 꺼져 있어도 울린다.** 꺼져 있다고 안 깨우면 알람이 아니기 때문이다.
- 끌 때까지 2초 간격으로 반복해서 운다. 소리를 못 내도 화면 오버레이는 그대로 뜬다.
- 알람이 울릴 시점엔 클릭이 없어 오디오 컨텍스트를 깨울 수 없으므로, 모드 진입(클릭) 시점에 미리 깨워 둔다.
- 출력 장치는 구분하지 않고 시스템 기본 출력으로만 재생한다(`setSinkId` 미사용).

---

## 10. 호감도

### 구현 완료

| 항목 | 현재 구현 |
| --- | --- |
| 저장 값 | `affinity.affinityPoints` |
| 범위 | 0~100 |
| 사용자 이름 최초 설정 | +5 |
| 애완돌 이름 최초 설정 | +5 |
| 닦아주기 | 하루 1회, +3 |
| 쓰다듬기 | 하루 1회, +3 |
| 일일 리셋 | 오전 8시에 `dailyCleanDone`, `dailyPetDone` 초기화 |
| 3단계 진화 | 2단계 상태에서 90점 이상 |

이름 보상은 **최초 지정 1회뿐**이다. 이름을 바꾸거나 지워도 다시 주지 않는다(`userNameSetAt`·`petNameSetAt`이 플래그). 이름 보상만으로 90을 넘겨도 3단계 판정이 걸려야 하므로, 지급과 판정을 같은 함수에서 처리한다.

### 표시용 레벨

| 점수 | 레벨명 (ko / en) |
| --- | --- |
| 0~19 | 낯가림 / Shy |
| 20~39 | 서먹 / Awkward |
| 40~59 | 친근 / Friendly |
| 60~79 | 살가움 / Warm |
| 80~100 | 각별 / Devoted |

레벨은 저장하지 않고 `tray-pet.js`에서 점수로 파생한다. **2→3 진화 판정은 레벨과 무관하게 raw 90점을 쓴다.**

보류/제외:

- 레벨별 아이템 언락
- 추가 상호작용별 세부 쿨다운

---

## 11. 스킨

### 구현 완료

- 스킨은 별도 코스메틱이 아니라 진화로 해금한 각 단계의 모습이다.
- 0~3단계 스킨 칸을 표시한다.
- 현재 도달 단계 이하만 착용 가능하다.
- 미래 단계는 잠금 처리한다.
- 착용 중인 단계에는 "착용중" 배지를 표시한다.
- 실제 현재 단계 칸을 누르면 원래 모습으로 복귀한다.
- 진화 완료 시 이전 착용 스킨은 해제된다.
- 펫 창과 트레이 히어로 모두 착용 스킨을 반영한다.

---

## 12. 시스템 모니터

### 구현 완료

- `systeminformation` 기반 수치 조회
- SYSTEM 화면이 열려 있을 때만 2초마다 폴링
- CPU/RAM/디스크/배터리/네트워크 표시
- 게이지 색상 구간
- 상세 드롭다운
- CPU 부하별 반응 문구
- CPU 부하별 작은 펫 회전 속도 변경
- 작은 펫 이미지는 현재 진화/포즈 스프라이트와 동기화

남은 확인:

- 장시간 사용 시 폴링 비용 실사용 검증

### AI 사용량 (Codex만)

구현 완료:

- SYSTEM 화면에 CODEX 행 추가. 게이지·상세 드롭다운은 기존 지표와 같은 형태
- CPU·메모리와 달리 **폴링하지 않는다.** 창을 열 때 1회 + 수동 새로고침 버튼을 누를 때만 읽는다
- 상세에 "마지막 확인" 시각을 상대 시간("3분 전")으로 표시해, 값이 그대로여도 확인 여부를 알 수 있게 함
- 조회 경로는 `src/main/ai-usage.js` → `system:get-ai-usage` IPC. `reload()`가 던져도 조회 전체가 무너지지 않게 삼키고 직전 스냅샷을 유지한다(`test/ai-usage.test.js`가 고정)

**Claude Code는 지원하지 않는다 — 트레이 맨 아래에 "공식 연동 대기 중" 안내만 둔다.**
가져올 방법이 없어서다. 다시 시도하기 전에 아래를 먼저 확인할 것:

- 로컬 세션 로그(`~/.claude/projects/**/*.jsonl`)에는 `usage`(토큰 수)만 있고 `rate_limits` 키가 **없다**(2026-08-05 확인). Codex와 대칭이 아닌 이유가 이것
- 공개 사용량/한도 API는 전부 **분당 RPM·ITPM·OTPM의 API 조직 한도**다. 5시간·주간 창은 **구독(Pro/Max)** 개념이라 결제 주체가 아예 다르다 — API 키로 조회하면 사용자의 Claude Code 구독이 아닌 무관한 값이 나온다. `Rate Limits API`도 설정된 한도값 조회지 사용량 조회가 아니다
- 한도가 나오는 자리는 statusLine의 stdin뿐인데, 그러려면 **남의 앱 설정 파일(`~/.claude/settings.json`)에 손을 대야 한다.** 이 방식을 한 번 구현했다가(프롬프트 복사 → 앱이 직접 편집 순으로 두 번 설계) 유지 비용이 기능 가치를 넘어선다고 판단해 **전부 제거했다**(2026-08-05)
- 제거 대상이었던 것: `claude-statusline-installer.js`, `claude-usage-cache.js`, `scripts/install-claude-statusline.js`, `npm run install:claude-statusline`, `claude:*` IPC 3개, 트레이의 Claude 게이지·상세·연동 버튼, `.sys-setup*` CSS
- 트레이 행은 `sys-row-static`(접히지 않는 행)으로 CODEX 아래에 둔다. 값은 `—` 고정, sub만 "공식 연동 대기 중"

Codex (`src/main/codex-usage-cache.js`):

- 설치·설정이 필요 없다. Codex가 평소 동작하며 `~/.codex/sessions`에 남기는 `.jsonl` 세션 로그를 읽기만 한다
- 한도는 계정 단위라 **가장 최근에 쓰인 파일 하나만** 읽고, 거기서 마지막 `rate_limits.primary` 스냅샷(한도%·플랜·리셋 시각)을 취한다
- 읽은 오프셋을 캐시해 새로 붙은 줄만 스트리밍으로 읽는다(수 MB 세션도 전부 메모리에 올리지 않음). 파일이 줄면 잘린 것으로 보고, 새 세션 파일이 생기면 그쪽으로 갈아탄다
- 스냅샷이라 실시간 값이 아니다. `resetsAt`이 이미 지났으면 그 수치는 만료된 것이므로 퍼센트 대신 "갱신 필요"를 표시한다(Codex를 한 번 써야 새 값이 로그에 남는다)

---

## 13. 부가 리액션

### 구현 완료

- 활성 앱별 말풍선
- 사생활/민감 영역 앱·사이트 silent 처리
- 회의 앱 quiet 처리
- 개발 문서, AI 도구, 리서치, 일정/할 일, 지도, 배달, 게임 등 앱·사이트별 자동 말풍선
- SNS 체류 시간별 단계형 말풍선
- 배터리 부족 구간별 sleepy/sad 리액션
- 로컬 시스템 시각 기준 시간대 리액션
- idle time 기반 장시간 사용 리액션

### 시간대 기반 리액션

구현 완료:

- 위치 권한, 날씨 API, 네트워크 호출 없이 로컬 시스템 시각만 사용
- 시간대 구분: 아침 `05:00~10:59`, 낮 `11:00~16:59`, 저녁 `17:00~21:59`, 밤 `22:00~04:59`
- 클릭 말풍선에 시간대별 문구를 추가로 섞음
- 밤 시간대에는 배터리 부족 상태가 아닐 때 기본 포즈를 `sleepy`로 표시
- 앱 실행 중 시간대가 바뀌어도 1분 단위로 상태 갱신
- 사용자 이름이 필요한 문구는 저장된 사용자 이름이 있으면 `이름님`, 없으면 `주인님`으로 출력

### 장시간 사용 리액션

구현 완료:

- Electron `powerMonitor.getSystemIdleTime()`으로 최근 입력이 없는 시간을 확인
- 5분 이상 idle이면 쉰 것으로 보고 누적 사용 시간을 초기화
- 앱이 켜진 뒤 idle 없이 활동한 시간이 45분, 90분, 120분을 넘으면 휴식 권유 문구를 클릭 메시지 풀에 추가
- macOS Screen Time 데이터 직접 접근은 사용하지 않음

### 미구현/보류

- 날씨 기반 리액션은 제외
- 앱별 멘트 추가 확장

---

## 14. 다국어 (한국어 / 영어)

### 구현 완료

- 설정 화면의 언어 칩과 온보딩 화면 우측 상단의 `EN`/`한` 버튼으로 전환한다. 재시작 없이 즉시 반영된다.
- 저장 값은 `settings.language`(`"ko"` | `"en"`), 기본값 `ko`.
- 렌더러 문구는 `src/renderer/shared/i18n.js`의 `t(key, params)`. 정적 마크업은 `data-i18n` / `data-i18n-html` 속성을 `applyStaticI18n`이 채운다.
- 데이터 안의 문구(질문·말풍선·모드 라벨·호감도 레벨 등)는 `{ ko, en }` 객체로 두고 `pickText()`로 고른다.
- 메인이 직접 띄우는 것(배너 알림)은 메인에서 언어를 고른다 — `src/main/i18n.js`와 `i18n:get-locale-sync`.

### 주의점

- **저장 데이터의 키는 언어와 무관하다.** `traitScores`는 한글 키(`화강암` 등)로 고정이라, 언어를 오가며 답해도 점수가 흩어지지 않는다.
- `applyStaticI18n`은 `data-i18n-html` 요소의 innerHTML을 통째로 덮어쓴다. 그 안에 스크립트로 채워 넣은 값(앱 버전 등)이 있으면 언어 전환 때 지워지므로, 별도 `<span>`으로 빼야 한다.
- 온보딩 진행 중 언어를 바꿔도 장면이 멈추지 않아야 한다(1.0.1에서 고친 회귀).

---

## 15. 시스템 권한

macOS 권한 세 가지를 쓴다. **셋 다 없어도 앱은 동작한다.**

| 권한 | 쓰이는 곳 | 없으면 | 온보딩에서 |
| --- | --- | --- | --- |
| 화면 기록 | 활성 앱 이름·창 제목 읽기 (`active-win`) | 앱별 말풍선만 안 뜬다 | **필수** — 허용해야 시작 버튼이 열린다 |
| 자동화 (System Events) | Dock의 위치·크기 읽기 (`osascript`) | Dock 회피가 근사치로 동작한다 | **선택** — 거부해도 온보딩을 끝낼 수 있다 |
| 손쉬운 사용 | 청소·쪽잠 모드의 키보드 잠금 | 해당 모드에서 키보드가 안 잠긴다 | **묻지 않는다** — 모드에 들어갈 때 그 자리에서 요청 |

### 설계 근거

- **자동화를 필수로 두면 안 된다.** macOS는 자동화를 한 번 거부하면 다시 묻지 않는다. 필수로 두면 사용자가 온보딩에서 갇힌다.
- **손쉬운 사용은 온보딩에서 빼는 게 맞다.** Rockie 본체에는 필요 없고, 맥락 없이 요청 창이 뜨면 무슨 앱인지 알기 전에 거부당한다. 1.0.1에서 이렇게 바꿨다.
- 권한 화면의 자동화는 **조회만** 한다(`requestDockAutomation`이 아니다). 조회 자체가 시스템 권한 창을 띄우므로 별도 요청이 필요 없다.
- 화면 기록은 허용해도 앱 재시작이 필요할 수 있어, 권한 화면이 "다시 시작하기" 모드로 전환된다.

---

## 16. 캐릭터 리소스 구조

### 현재 구현

- 단계/돌/상태별 GIF 파일을 직접 사용한다. `assets/gif/level0~level3`.
- 파일명 규칙은 `<접두어>_<포즈>.gif`. 포즈는 `left`/`right`/`smile`/`sad`/`sleepy`이고, **`love` 포즈는 level0/level1에만 있다.**
- 매핑 정의처는 **`src/renderer/shared/sprites.js` 한 곳뿐이다**(`resolveSprite`·`spriteGifUrl`). pet·tray 렌더러가 공용으로 쓰며, 렌더러에서 다시 선언하지 않는다.
- level2/3의 애정 표현은 포즈 대신 공용 `assets/gif/heart.gif`를 캐릭터 위에 겹쳐서 처리한다. 겹치는 위치는 `pet-data.js`의 스프라이트별 보정값으로 잡는다.
- `assets/img/`의 PNG는 트레이 히어로·배너용 정지 이미지다(전체 조합이 아니라 필요한 것만 있다).

### 보류

- GIF → PNG 시퀀스 + Canvas 렌더링 전환
- 팔레트 스왑
- 레이어 합성
- 모자 anchor 좌표
- 빌드 타임 자산 생성

보류 이유:

- 현재 단계별 GIF 방식으로 요구 기능은 동작한다.
- 모자 등 조합형 아이템을 실제 착수할 때 구조 전환하는 편이 단순하다.

---

## 17. 채팅

### 보류

- 최종 모델 선택
- `node-llama-cpp` 연동
- `chat:send` → `chat:token` → `chat:done` IPC
- 돌 종류별 말투 프롬프트
- 최근 대화 컨텍스트 관리
- 오래된 대화 요약 압축

채팅 기능은 구현되어 있지 않고, 저장 스키마에서도 `chat` 영역을 뺐다(아무도 읽지 않는 자리였다).
시작할 때 `store.js`의 `defaultData()`에 다시 추가하면 `load()`가 기존 사용자 파일에 자동으로
채워주므로, 미리 자리를 잡아둘 필요는 없다.

설계안은 두 곳에 남겨 뒀다. 착수하게 되면 조사부터 다시 하지 않아도 된다.

- 모델 선택·추론 엔진·IPC·배포 전략: `docs/chatting.md`
- 대화 기록 저장 스키마(최근 N턴 + 주기적 요약): `docs/dataschema.md` §4

---

## 18. 배포와 자동 업데이트

**절차·서명·QA 체크리스트는 `docs/release.md`가 정본이다.** 여기서는 현재 상태만 적는다.

### 현재 상태 (2026-08-21, v1.0.2)

- macOS 전용. Developer ID 서명 + notarization + stapling 완료.
- 산출물은 `dmg`(사람이 내려받는 용도)와 `zip`(업데이터 전용) 두 개 + `latest-mac.yml`.
- 배포처는 **GitHub Releases**. 산출물이 약 124MB라 정적 호스팅(Cloudflare Pages 25MB 제한)에 못 올린다.
- 다운로드 페이지와 정책 3종은 `jeondowon.com/rockie`에 있고, 앱 설정 하단에서 링크로 연다.
- 릴리스는 `./scripts/release.sh` 한 줄. 릴리스 본문은 `docs/release-notes.md`의 해당 버전 절을 잘라 쓴다.

### 자동 업데이트 (`src/main/updater.js`)

- 개발 실행에서는 아예 돌지 않는다(`app.isPackaged` 가드). 검증은 패키징된 앱에서만 가능하다.
- 시작 1분 뒤 + 이후 6시간마다 확인한다. 종일 켜두는 앱이라 시작 시 1회로는 부족하다.
- 다운로드는 백그라운드 자동. 다 받으면 배너 알림 1회 + 트레이 메뉴에 "업데이트 설치 후 재시작" 항목이 뜬다.
- **설치 시점은 사용자가 고른다.** Dock 아이콘이 없는 앱이라 트레이가 유일한 접점이기 때문이다.
- `autoInstallOnAppQuit` 기본값 `true` 그대로라, 항목을 안 눌러도 앱을 껐다 켜면 적용된다.
- 오프라인·서버 점검은 정상 상태로 보고 조용히 넘긴다.
- 2026-08-21 1.0.0 → 1.0.1로 실제 갱신 검증 완료.

### BM

- 후원: 사이트에 CTEE 링크. 앱에서는 PET 화면의 후원 카드와 설정 하단의 홈페이지 링크로 연결한다.
- 유료 아이템 없음. 약관 11조에 "대가 없는 자발적 지원 · 특전 없음"으로 못박았다.

---

## 19. 검증 상태

### 자동 테스트

`npm test`(`node --test`) 기준 **32개 전부 통과**한다.

`test/evolution.test.js` (19개):

- 본 질문 완료 후 원석 1단계 확정
- pending 진화 연출 완료 시 대기 해제 + 완료 단계 기록
- 진화 시작 시 스킨 해제, 카드가 끝날 때까지 스킨 잠금
- 본 질문 2개 돌 동점 타이브레이커
- 본 질문 3개 돌 동점 타이브레이커 순차 삽입
- E/I 질문 완료 후 변성체 2단계 확정
- E/I 3:3 동점 타이브레이커
- 2단계 진입 시 호감도 90 이상이면 즉시 3단계
- 닦기/쓰다듬기로 90점 도달 시 3단계
- 이름 보상으로 90점 도달 시 3단계 / 못 미치면 진화 안 함
- 이름 보상은 최초 1회뿐 (변경·삭제해도 재지급 없음)
- 이름 대상이 user/pet이 아니면 무시
- 2단계 이상에서 일일 갱신 후 질문 배지 제거
- 성향 태그 계산
- 온보딩 질문 점수 반영 + 완료 후 오늘 질문 3개 채움
- 표시용 진행도 22개 계산

`test/store.test.js` (2개):

- 진행 중인 온보딩은 재시작해도 완료 처리되지 않는다
- 온보딩을 시작한 적 없는 기존 사용자는 완료 처리된다

`test/ai-usage.test.js`:

- 조회할 때마다 파일을 다시 읽어 퍼센트 갱신
- 한쪽 파일을 읽지 못해도 다른 쪽은 계속 표시

`test/codex-usage-cache.test.js`:

- 오늘 토큰만 합산하고 최신 rate_limit 스냅샷 사용
- 증분 읽기 결과가 전체 재독과 동일, 끊긴 마지막 줄은 다음 갱신까지 보류
- 파일 잘림·교체 시 처음부터 재독, 날짜가 바뀌면 어제 누계 폐기
- 이벤트 원본을 메모리에 쌓지 않음, 오늘 쓰지 않은 파일은 캐시에서 제거

### 자동 테스트가 없는 영역

렌더러(`pet.js`·`tray/*`)와 메인의 창·트레이·권한 코드에는 테스트가 없다. Electron 런타임이 필요해서다. 아래는 눈으로 확인해야 한다.

- 온보딩 전체 UX와 권한 화면 흐름
- 오전 8시 리셋과 OS 배너 알림 (**개발 실행에서는 알림이 전부 실패한다** — Electron 43의 `UNUserNotificationCenter`가 서명을 요구한다. 코드 버그가 아니므로 재디버깅하지 말 것. 서명된 배포 빌드에서만 확인 가능)
- 진화 카드 애니메이션과 스킨 전환
- 세 모드의 잠금·타이머·알람, 권한 없는 상태의 안내 카드
- 언어 전환 시 각 화면의 문구
- SYSTEM 화면 장시간 폴링 비용
- 확장 디스플레이 환경(현재 주 모니터에만 잔류하는 것이 의도된 동작)

---

